# papr.site — robots.txt

# Allow all crawlers full access
User-agent: *
Allow: /

# Block nothing — all pages are public and indexable
Disallow:

# Sitemap location
Sitemap: https://papr.site/sitemap.xml
